import { combineReducers } from "redux"

const initState = {}

export const candlesReducer = (state: any = initState, action: any) => {
    return state
}

export const reducer = combineReducers({
    candlesReducer
}) 
